import 'package:flutter/material.dart';

class Dashboard_screen extends StatelessWidget {
  const Dashboard_screen({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(body: Text('hello flutter'),);
  }
}
